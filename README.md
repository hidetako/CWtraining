# CW 交信トレーニング

ブラウザだけで動く、モールス符号（CW）の受信・送信・実戦運用の練習アプリです。
インストールもアカウント登録も API キーも不要で、`index.html` を開けばすぐ使えます。

FB NEWS の記事「[生成AIとCWで交信practice](https://www.fbnews.jp/202408/special2/index.html)」
（7M4MON 野村秀明氏）の着想と、CW コンテストシミュレーター
[Morse Runner](https://github.com/VE3NEA/MorseRunner)（VE3NEA）の設計を参考にしています。
元記事は Python + OpenAI API + ffmpeg の構成ですが、本アプリは交信シナリオをローカルで
生成し、音は Web Audio API で合成するため、外部サービスに一切依存しません。

## 使い方

操作方法は **[使い方説明書（docs/MANUAL.md）](docs/MANUAL.md)** にまとめています。
習熟度別のおすすめ順路、各タブの操作、キーボード操作の一覧、
困ったときの対処が載っています。

配布用の Word 版も同梱しています
（[CW交信トレーニング_使い方説明書.docx](docs/CW交信トレーニング_使い方説明書.docx)、A4 20 ページ）。
本文は Markdown 版が原本で、Word 版は次のコマンドで生成し直せます。

```bash
npm install docx
node docs/build-manual-docx.js
```

### ローカルで動かす

ES モジュールを使っているため、`file://` で直接開くとブラウザに読み込みを拒否されます。
簡易サーバー経由で開いてください。

```bash
python3 -m http.server 8000
# ブラウザで http://localhost:8000/ を開く
```

GitHub Pages などの静的ホスティングに置いた場合は、そのまま URL を開くだけで動きます。

## 機能

### 交信シミュレーター

CQ を出すところから `73` で終わるまで、ラバースタンプ QSO を一通り体験します。
習得段階に合わせて 3 つのスタイルがあります。

**ガイド付き模擬交信**（型を覚える段階）

各段階で「今どの局面か」「何のための送信か」を示し、自分が送る番では
**3 つの選択肢から選びます**。選ぶと、正解だけでなく他の選択肢がなぜ駄目なのかも
表示されます。距離のある間違い方ではなく、初心者が実際にやりがちな誤りを出します。

- 相手のコールサインを付けずに送ってしまう
- `DE` と自分のコールサインを落とす
- まだ交信が続くのに `<SK>`（交信終了）を打ってしまう
- 呼ぶ段階でいきなり交換内容を送ってしまう
- レポートを交換する前に `73` を出してしまう

相手の送信では、まず音だけを聞かせ、そのあと本文・用語の意味・
**その反応が何を意味し、次に何をすべきか**を表示します。

選択肢に答えたあとは「実際に送る内容」の下に **「パドルで打ってみる」** の枠が出て、
その内容を自分で打てるかをその場で試せます（任意）。打った符号は解読されて表示され、
「お手本と照合する」で文字単位の差分が出ます。打たずに次へ進んでもかまいません。

**実技（パドルで打つ）**（型を手に覚えさせる段階）

解説を見ながら、自分の送信は実際にパドルで打つモードです。呼び出しから `73` までを
通しで運用します。ターンごとに手本と照合して採点し、終了時に平均一致率が出ます。
ガイド付きの打鍵欄と違い、こちらは成績が記録に残ります。

**聞き取り練習**（実力を試す段階）

相手の送信を聞き取り、ログシート（コールサイン・RST・名前・QTH・リグ・アンテナ・天気）に
書き取ると、項目ごとに採点されます。各欄には何を入れるのかが表示され、欄そのものも
周りより明るく大きく取ってあります。**受信中は相手の送信を画面に出しません**（既定）。
流れている文字や語の意味が見えると答えが分かってしまうためで、
必要なら「受信中に相手の送信を表示する」で入にできます。

**相手の反応のゆらぎ**

型どおりに進むだけでなく、実際の交信で起こることを差し込めます。
「おまかせ」にすると毎回変わります。

| 反応 | 相手の送信 | 意味 |
|---|---|---|
| 名前を聞き返される | `PSE NAME AGN?` | 名前だけ取れなかった。名前だけ繰り返せばよい |
| ゆっくり頼まれる | `QRS PSE QRS` | 速すぎる。速度を落として送り直す |
| コールサインを取り違えられる | 別のコールで呼ばれる | 訂正しないと相手のログが誤ったまま残る |
| 呼び出しを取ってもらえない | `QRZ?` | 自分のコールをもう一度打つ |
| 手短に切り上げたい | `SRI QRU PSE 73` | 設備の話は省いて締めに向かう |

**型の早見表**

パネル内にいつでも開けるチートシートがあります。呼び出しから締めまでの
6 段階について、送信例・各語の意味・その段階のコツをまとめています。
送信例には自局のコールサインが当てはめられます。

### 聞き取りドリル
- **コッホ法** — 2 文字から始めて、正答率 90% を超えたら 1 文字ずつ増やします
- **頻度順** — 英文での出現頻度が高い文字から練習します
- **コールサイン / 略語・Q 符号 / Q 符号だけ / 数字・RST / RST レポート / 頻出単語 /
  名前 / 地名（QTH）/ 設備（RIG・ANT・出力）/ 記号・プロサイン**
- **交信の定型文 / 実戦の一節** — 単語ひとつずつ取れても続けて送られると取れない、
  という段差を埋めるためのものです。`UR 599 599 NAME BOB BOB` のように、
  実際に送られる並びのまま出題します

答え合わせでは、答えに含まれる **Q 符号・略語・RST・プロサインの意味と符号**を並べます
（`QRM 混信を受けている －－・－ ・－・ －－`）。RST は各桁まで分けて説明します。

`Enter` は「採点する」と「次の問題へ」を兼ねますが、**採点した直後の `Enter` は
受け付けません**。書き終えた勢いで押し込んだり長押しすると、採点結果を見ないまま
次に移ってしまうためです。「レベルを 1 上げる」もその場では次の問題に進みません。

「出題する」を押すと **3 → 2 → 1** と数えてから流れます。押した瞬間に鳴ると
身構える間が無く、最初の 1 文字を落としやすいためです。連続出題の「次の問題」でも
毎回数えます。「もう一度聞く」「ゆっくり再生」は、すでに構えているので数えません。

再生を押すと**書き取り欄にカーソルが移り、そのまま打ち込めます**。書きかけの内容は
消えず、カーソルは末尾に付くので続きから打てます。

文字単位で採点し、間違えた文字は「苦手な文字」として蓄積されます。採点は最長共通
部分列で対応を取ってから過不足を数えるので、途中で 1 文字取り漏らしても以降が
全部ずれて誤り扱いになることはありません。**書き間違いは 1 回の誤り**として数えます
（落としたときと同じ重さ。最長共通部分列は取り違えを「取り漏らし＋余分」に分けて
出すので、そのままでは 1 回の誤りが二重に効いてしまいます）。書き間違いではない
余分は分母に加えるため、当てずっぽうで文字数を増やしても得点は上がりません
（打鍵の採点も同じ方式です）。
打鍵側では語の間隔を照合に使いません。解読は手が止まっただけで語間を入れるので、
間隔まで採点すると符号そのものの正誤が見えなくなるためです。

照合は文字の見た目ではなく**符号で**行います。表記が違っても符号が同じものは打てば
区別が付かないためです。`=` と `<BT>` はどちらも `-...-` なので、手本の `=` を
`<BT>` と解読しても正解として数えます（`+` と `<AR>`、`(` と `<KN>` も同様）。

### コンテスト運用（Morse Runner 準拠）

[Morse Runner](https://github.com/VE3NEA/MorseRunner)（VE3NEA）の設計を移植しています。
相手局は「コールサインを再生する機械」ではなく、状態を持ったオペレーターとして動きます。
取り違えれば訂正を求め、待たせすぎれば忍耐が尽きて去ります。

**運用モード**

| モード | 内容 |
|---|---|
| パイルアップ | CQ に複数局が同時に応答。取る力を鍛える基本モード |
| シングルコール | 1 交信につき 1 局。入力操作に慣れるためのモード |
| WPX コンテスト | 条件固定・30 分。プリフィックスがマルチになり、得点 = 有効 QSO × マルチ |
| HST 競技 | IARU 高速電信競技の形式。妨害の無い条件で速度と正確さを競う |

**難易度（受信ヘルプ）**

相手が送っている内容をどこまで画面に出すかを 3 段階で選べます。

| 段階 | 表示 |
|---|---|
| 初級 | 送られた内容を順に表示。読んでログに打ち込むまで残ります |
| 中級 | いま打たれている 1 文字だけ。文字は残りません |
| 上級 | 表示なし（既定。実際のコンテストと同じ条件） |

**相手のコールサインはどの段階でも伏せます**（`？` と表示。文字数も伏せます）。
聞き取るべき答えそのものなので、出すと練習になりません。RST・ナンバー・定型
（`CQ` `TEST` `DE` `R` `TU` など）と自局のコールサインは表示します。混信局の送信は
表示しません。運用中でも切り替えられます。

**ファンクションキー**

| キー | 動作 |
|---|---|
| `F1` | CQ |
| `F2` | 相手のコールとナンバーを送信 |
| `F3` | TU（交信確定・ログ記録） |
| `F4` | 自局コールサイン |
| `F5` | 相手コールサイン |
| `F6` | QSO B4（重複交信の指摘） |
| `F7` | `?`（再送要求） |
| `F8` | `NR?`（ナンバー再送要求） |
| `Enter` | 状況に応じて F2 / F3 |
| `Space` | 呼んでいる局からコールサインを補完 |
| `Tab` | 入力欄の移動 |
| `Esc` | 送信を止めて入力欄を消去 |
| `↑` `↓` | RIT を回す |

**相手局の速度**

呼んでくる局の速度を、自局の速度とは別に設定できます（10〜45 WPM）。局ごとの
ばらつき（0〜±12 WPM）も指定でき、0 にすると全局がそろいます。運用中の変更は
次に CQ を出したところから効きます — すでに呼んでいる局の速度は変わりません
（実際の交信でも相手の手は変えられないため）。

**相手オペレーターの状態機械**

`osNeedQso → osNeedNr → osNeedCall / osNeedCallNr → osNeedEnd → osDone`（または `osFailed`）
と遷移します。打ち込んだコールサインは 3 段階で照合され、完全一致なら交換に進み、
惜しい場合は相手が自分のコールを打ち直して訂正を促します。応答が無いまま
待たされると忍耐度が減り、尽きると諦めて去ります。

**LID（下手な運用者）**

`LID` を有効にすると、一部の局が次のような癖を持ちます。

- 他局の交信中に割り込んで呼ぶ
- 符号を打ち間違える
- ありえない RST（339 など）を送ってくる
- 正しく取ったのに訂正を求めてくる
- 自分宛でないのに応答してしまう
- 何度も同じことを繰り返す

**受信系**

- **RIT** — ±500 Hz。回すと周波数のずれた局ほど音程が動きます
- **受信帯域幅** — 100〜2000 Hz。バンドパスを 2 段重ねた CW フィルタで、絞ると帯域外の局が落ちます
- **QSK** — オフにすると、自局の送信中は受信音が絞られます
- **QRM** — 別の周波数で延々と CQ を出している局として実装しています

**採点**

Morse Runner の `CheckErr` と同じ優先順位で、NIL（該当局なし）→ コールサイン違い →
DUP（重複）→ RST 違い → ナンバー違い を判定します。素点（打ち込んだ全交信）と
確定点（誤りを除いたもの）を並べて表示し、5 分ごとの交信数をヒストグラムにします。
毎時ペースは直近 5 分間の窓で算出します。運用ごとのハイスコアも残ります。

運用そのものを録音して保存することもできます。

### パドル送信（エレクトロニックキーヤー）
パドルをマウスの左右クリックに変換して接続している環境を想定しています。
キーボードの `Z` / `X`、タッチ画面では打面の左右半分でも打てます。

- **利き手** — 右手用（左ボタン＝短点）／左手用（右ボタン＝短点）
- **モード** — アイアンビック B / アイアンビック A / ウルティマチック / 縦振り電鍵
- **ウェイト** — マークとスペースの比率（35〜65%、標準 50%）
- **速度** — 5〜28 WPM（手で崩れずに打てる範囲に練習を収めるため上限を 28 にしています。
  コンテスト運用の「自局速度」も同じ上限です）
- 打った符号はその場で解読され、課題（コールサイン・略語・定型文）と文字単位で照合します
- 打った符号の下に「採点する・打ち直す・次に進む」が並びます。**「次に進む」は常に押せ**、
  採点を挟まずに次の課題へ移れます（手応えの無い打鍵を採点に通す必要はないため）
- **押されたままにならない仕組み** — 押したまま別のアプリに切り替えるなどすると
  「離した」合図が届かず、短点が延々と入り続けていました。ウィンドウが焦点を失った
  時点ですべて離し、タッチは押した場所で側を決めます（離した場所で決め直すと、指を
  滑らせたときに押していない側を離すことになります）。塞ぎ忘れに備えて、10 秒以上
  押されたままなら自動で止め、打面の下に断り書きを出します
- **交信の定型文は話題別に 69 文**。呼び出し・応答／基本の交換／天気／時刻／場所・地理／
  電波の状況／フェージング／締めくくり／聞き返し・トラブルから選べます（おまかせも可）。
  自局の設定（コールサイン・名前・QTH・リグ・出力・アンテナ）が差し込まれます
- 課題に Q 符号・略語が含まれるときは、その下に短い意味が並びます。何を送るのか
  分からないまま打つことにならないようにするためで、語ごとの色分けとホバーの
  詳しい説明も付きます

#### 右手用・左手用について

パドルを同じ向きに置いたまま、右手で操作すると**左のレバーに親指**が、
左手で操作すると**右のレバーに親指**が当たります。CW では親指を短点に
割り当てるのが一般的なため、左手で打つ場合は短点と長点の割り当てを
右手用とは逆にする必要があります。「利き手」を切り替えると、この入れ替えが
マウスボタン・キーボード・タッチのすべてに反映され、パドル欄の表示も
追従します。

親指を長点にしたい場合は「親指側を長点にする」も併用できます。
利き手と組み合わせて 4 通りの割り当てから選べます。

| 利き手 | 親指 | 左ボタン | 右ボタン |
|---|---|---|---|
| 右手用 | 短点 | 短点 | 長点 |
| 左手用 | 短点 | 長点 | 短点 |
| 右手用 | 長点 | 長点 | 短点 |
| 左手用 | 長点 | 短点 | 長点 |

パネル先頭に **13 ステップのチュートリアル**があります。短点・長点の連続送出から
スクイーズ、モード A と B の違い、ウェイトと速度の合わせ方まで、実際に打って
条件を満たすと自動で次に進みます。

### CW 交信サポート（無線機と接続）

無線機の受信音からモールスを解読し、返答の候補まで出す実運用の補助機能です。

- **デコーダー** — 受信音をマイクかライン入力で聞かせると文字に起こします。
  トーン周波数はワンタッチで自動合わせ、速度（WPM）は受信しながら自動追従。
  バンドパス＋帯域内比のゲートで、合わせた周波数以外のトーンや音声は拾いません
- **情報の自動抽出** — 相手のコール（`DE` の次）・もらった RST（`5NN` → 599）・
  名前・QTH が自動で埋まります。手直しも可能
- **返答の候補** — 交信の段階（CQ を聞いた → 呼ばれた → 交換 → 締め）に合わせて
  候補を出し、選んで直して送れます
- **自分のパドルで打つ** — 候補を手本として表示し、見ながら自分の電鍵で打てます。
  打った内容は手本と照合され、実際に打った符号が交信記録に残ります。
  返答を考えることと打つことを分けられるので、実交信に出る不安を減らせます
- **自動送信** — キーイングされたトーン音を出します（SSB モード＋VOX などで
  無線機へ）。**自動応答**（実験的・既定オフ）は 2 秒の予告つきで取り消せます
- **シリアルキーイング**（実験的・Chrome 系のみ） — Web Serial の DTR/RTS で
  キーイング回路ごしに電鍵端子を直接叩きます（実機検証はまだです）
- **ログ連携** — 拾った情報＋受信・送信の交信記録を 1 クリックでログ帳へ登録

### ログ帳

実際の交信を記録する台帳です（練習の成績とは別）。一般的なログソフトの
基本機能を備えています。

- 登録・編集・削除。同じ局を入れると**交信歴**が出て、名前・QTH・JCC が
  前回の値で埋まります。周波数からバンドを自動判定（7.010 → 7MHz）
- **JCC・JCG 検索** — JARL の市郡区番号リスト（都道府県 47・市 890・郡 586・
  区 207 件、消滅済み含む）を収録。番号でも市郡区名（漢字・ローマ字）でも引けます
- **現在地から JCC を探す** — ブラウザの位置情報から近い市郡を距離順に表示
  （代表点座標は GeoNames と突き合わせて生成）。移動運用先で自分の
  JCC / JCG を調べられます。政令指定都市では区番号も一緒に出ます
- 検索・バンド/モード絞り込み、JST / UTC 表示切り替え
- **ADIF / CSV の書き出し・取り込み**（ADIF は他ログソフトや QSL 転送サービスと
  やり取りできる標準形式。日時は UTC、バンドは 40m 表記）
- 集計 — 交信数・ユニーク局・JCC / JCG ワークド・QSL 受領・バンド別・モード別

保存はブラウザの localStorage。書き出しで控えを取れます。

### 初心者モード
送信中の Q 符号・略語・RST・コールサインを、その語が流れているタイミングで
リアルタイムに解説します。本文中の用語は種類ごとに色分けされ、マウスを乗せると
意味が出ます。RST は「599 = 了解度5:完全 / 強度9:極めて強い / 音調9:完全な純音」のように
桁ごとに展開されます。

説明には**実際の符号**を添えてあります（`FER` → 「〜のために (for)　・・－・　・　・－・」、
`DE` → 「〜から（こちらは）　－・・　・」）。意味だけ分かっても打てないためです。
文字ごとに区切って並べてあるので、どこで手を止めるかも読み取れます。

聞き取りの答えが部分的に見えるため、実力を試すときは設定でオフにしてください。

本文を表示したあとに聞き直すと、**今どの語を送っているかが語単位で光ります**。
取りこぼした箇所を音と文字で突き合わせられます。初心者モードとは独立していて、
解説を切っていても光ります。

### バンドコンディション
QRN（空電）・QSB（フェージング）・QRM（混信）・フラッターを個別に切り替えられます。
フェージングは局ごとに独立してかかり、混信は別周波数で CQ を出し続けている局として
鳴ります。

### 画面構成
- **右側は常にパドル欄** — ヘッダーとタブの下から画面下端まで。打面・送信速度・
  打った符号の解読・要素の流れを、どのタブでも表示します。たたむ操作は持ちません。
  **打面はこの 1 か所だけ**で、パドル送信タブに二つ目の打面は置いていません
- **ヘッダーとタブは固定** — 本文とパドル欄がそれぞれ内側でスクロールします
- **打ち直しは手近に** — 打面のすぐ下に「打ち直す」を置いています。`Esc` でも同じで、
  打った符号が残っていれば打ち直し、無ければ練習の終了として働きます。
  **打ち直しの `Esc` は焦点がどこにあっても効きます** — 速度つまみを動かした直後でも、
  書き取り欄に打ち込んでいる最中でも同じです（つまみ自体が `<input>` なので、
  「入力欄では効かせない」という作りだと、速度を変えただけで効かなくなっていました）。
  一方、練習を終える側の `Esc` は取り消しが利かないため、文字を打ち込んでいる
  最中には効きません
- **一時停止・再開・終了** — 画面上部の 2 つのボタンで、どのタブでも同じように効きます。
  一時停止は AudioContext ごと止めるので、再開すると符号の途中から続きます。
  終了はタブごとに練習を開始前の状態へ戻します。`Space` / `Esc` でも操作できます

### その他
- **変換・電鍵** — 任意の文章をモールスに変換して再生、縦振り電鍵の練習
- **略語集** — 90 語の Q 符号・略語に加え、記号・プロサイン 11 種の対訳。
  符号（・－ 表記）を添え、クリックで音を確認できます
- **記号・プロサイン** — `=` `/` `?` `.` `,` `<AR>` `<SK>` `<KN>` `-` `<BK>` `@` を
  専用ドリルで練習できます。頻度順ドリルにも数字のあとに同じ並びで入っています。
  答案欄の下に入力ボタンが出るので、`<AR>` の書き方を知らなくても入れられます
  （ボタンの出し分けは「そのレベルで使う文字」で決めます。出題の中身では決めません —
  それだとボタンが出ていること自体が答えの手がかりになります）
- **記録** — ドリル・交信・送信・コンテストの成績と苦手な文字を localStorage に保存

## 速度の考え方

文字速度（char WPM）と実効速度（effective WPM）を別々に設定できます。
ARRL 方式のファーンズワース・タイミングで、**各文字は速いまま、文字間と語間だけを
広げます**。文字の響きを速度ごと覚え直さずに済むため、初学者はこの方式が推奨されます。

例えば文字速度 20 WPM・実効速度 10 WPM なら、一つ一つの文字は 20 WPM の速さで
鳴りますが、全体としては 10 WPM のペースになります。慣れてきたら実効速度を
文字速度に近づけていってください。

## ファイル構成

```
index.html          画面構成
icon.svg            アイコン（原本）— 中央はモールスの CW、上下は広がる電波
favicon.svg         タブ用の簡略版（16〜32px で潰れないよう弧を減らし滲みを外した）
apple-touch-icon.png / icon-512.png   icon.svg から書き出した PNG
css/style.css       スタイル
js/
  morse.js          符号表、トークン化、ファーンズワース・タイミング計算
  audio.js          Web Audio による音声生成（受信フィルタ・RIT・QSK・録音を含む）
  data.js           コールサイン・地名・リグ・略語などの語彙
  qso.js            交信台本の生成、相手の反応のゆらぎ、採点
  qsoguide.js       交信の型の知識（段階の説明・選択肢生成・反応の読み方）
  drills.js         ドリルの問題生成と採点
  contest.js        コンテスト運用エンジン（相手オペレーターの状態機械・LID 動作・難易度の伏せ方）
  contestlog.js     コンテストのログ・採点・統計
  keyer.js          エレクトロニックキーヤーと解読
  tutorial.js       パドル操作チュートリアルのステップ定義
  explain.js        Q 符号・略語のリアルタイム解説
  stats.js          設定と成績の保存
  app.js            画面の組み立てとイベント配線
docs/
  MANUAL.md         使い方説明書（利用者向け）
  build-manual-docx.js  MANUAL.md から Word 形式を生成する
tests/
  run-e2e.sh        E2E テストの実行スクリプト
  e2e/*.mjs         スイート本体（Playwright）
```

相手局の応答は `js/qso.js` の `LocalResponder` が乱数と語彙表だけで生成しています。
`buildScript(profile, options)` を実装したクラスに差し替えれば、元記事のように
LLM を相手役にすることもできます。

## テスト

実際の Chromium を自動操作して、全機能を通しで動かす E2E テストがあります。
モールス音の生成やキーヤーのタイミングはブラウザの中でしか再現できないため、
単体テストではなく実ブラウザで検証しています。

必要なもの: Node.js 18 以上、`python3`、Playwright + Chromium。

```bash
# 作業ツリーをそのまま検証する
tests/run-e2e.sh

# スイート名を指定して一部だけ実行する
tests/run-e2e.sh core keyer-tutorial

# 公開中のページの実ファイルを取り寄せて、そのバイト列を検証する
BASE=https://hidetako.github.io/CWtraining tests/run-e2e.sh --remote
```

`npm test` でも同じものが走ります。説明書の Word 版を作り直すときは
`npm run docs`（`docs/MANUAL.md` を編集したあとに実行してください）。

`--remote` は公開ページのファイルを一時ディレクトリへ取得してから
ローカルで配信します。ブラウザから直接公開 URL を開くのではなく取得した
実体を検証する形にしているのは、ネットワーク制限下でも同じ手順が通るように
するためです。検証対象は公開中のファイルそのものなので、
「手元では動くが公開版では動かない」を検出できます。

| 環境変数 | 既定値 | 用途 |
|---|---|---|
| `BASE` | `http://localhost:8123` | `--remote` のときの取得元 URL |
| `PORT` | `8123` | 検証用サーバーの待ち受けポート |
| `PW` | Playwright の既定の場所 | `playwright/index.mjs` の絶対パス |
| `SHOTS` | 一時ディレクトリ | スクリーンショットの出力先 |
| `VERBOSE` | 未設定 | 設定すると各スイートの出力を全部表示する |

各スイートは検出した不具合の数を終了コードとして返します。
ページ上の JavaScript エラーと `console.error` も失敗として扱うので、
表示は正しいが裏でエラーが出ている、という状態も落ちます。

| スイート | 検証する範囲 |
|---|---|
| `core` | 交信シミュレーター・ドリル・コンテスト・キーヤー・略語集・初心者モードの通し |
| `guided-qso` | ガイド付き模擬交信の選択肢・解説・型の早見表 |
| `guided-keying` | ガイド付きの「パドルで打ってみる」欄（打鍵・照合・打ち直し） |
| `live-qso` | 実技モードで実際に打鍵して採点まで進む |
| `contest` | Morse Runner 準拠の運用モード・採点・自己ベスト |
| `keyer-tutorial` | チュートリアル 13 ステップの進行と達成判定 |
| `paddle-hand` | 利き手・親指の割り当てと保存 |
| `tx-tone` | 送信音の周波数設定と受信音からの独立 |
| `wpm-cap` | 送信速度の上限 28 WPM（つまみ・API・保存値・コンテストの自局速度） |
| `layout-transport` | パドル欄の位置と大きさ、一時停止・再開・終了、聞き取りの既定表示 |
| `contest-dx-wpm` | コンテストの相手局速度（基準・ばらつき・運用中の変更・HST） |
| `grading` | 採点の妥当性（取り漏らしで以降がずれない・余分を見逃さない） |
| `keyer-task-terms` | パドル送信の課題に添える Q 符号・略語の意味 |
| `key-phrases` | 交信の定型文（話題別の出題・差し替え・打てる符号かの検査・保存） |
| `symbols` | 記号・プロサインの出題と採点（入力補助・同符号の等価・答えを漏らさないこと） |
| `keyer-next` | パドル送信の「次に進む」（採点を挟まずに進めること・ボタンの並び） |
| `keyer-timing` | 打鍵の反応（詰まっても要素が消えないこと・側音の経路） |
| `drill-countdown` | 出題までの 3 秒（数える対象・取り消し・聞き直しは待たせないこと） |
| `mobile` | スマホ画面（横はみ出し・引き出しの開閉・タップ打鍵・押せる大きさ） |
| `drill-focus` | 再生を押したら書き取り欄でそのまま打てること（焦点・入力の保持） |
| `diff-legibility` | 採点結果が読めること（字の上に線を引かない・内訳を言葉でも出す） |
| `drill-compare` | 間違えたときの見比べ欄（自分の答えと正解の 2 段・桁ぞろえ） |
| `drill-types` | ドリルの種類（15 種の出題・鳴らせない文字が無いこと・答え合わせの解説） |
| `drill-advance` | 採点を飛ばして次へ進まないこと（Enter の連打・レベル上げ） |
| `esc-consistency` | Esc が焦点の置き場所で変わらないこと（打ち直し・終了・Z/X） |
| `keyer-stuck` | レバーが押されたままにならないこと（焦点喪失・指の滑り・見張り） |
| `logbook` | ログ帳（登録・交信歴補完・JCC/JCG 検索・ADIF/CSV 往復・集計・永続） |
| `decoder` | CW 音声デコーダー（実音の解読・速度とトーンへの追従・混信を拾わないこと） |
| `support` | 交信サポート（情報抽出・返答候補・自動応答・ログ連携・シリアルの時系列） |
| `logsheet` | 聞き取り練習の書き取り欄（案内・目立ち方・開始ボタンとの距離） |
| `word-highlight` | 聞き直し中に今の語が光ること（順序・消灯・初心者モードと独立） |
| `redo-keying` | 打ち直しの導線（ボタンの位置と表示・Esc の使い分け・採点結果の片付け） |
| `term-code` | 説明に添える符号（・－ 表記）と、= / <BT> を同じ符号として採点すること |
| `contest-hint` | コンテストの難易度 3 段階（伏せ方・順次表示・1 文字表示・混信局の除外） |
| `icons` | アイコンの配信と描画（SVG が壊れていないか） |
| `improvements` | タブのキーボード操作、記録の書き出し・読み込み、推移グラフ、苦手集中ドリル |

## 動作環境

Web Audio API に対応したブラウザ（Chrome / Edge / Firefox / Safari）で動作します。
音を鳴らすには最初に画面のどこかを操作する必要があります（ブラウザの自動再生制限のため）。
設定と成績はブラウザの localStorage に保存され、外部には送信されません。

**パソコンとスマートフォンの両方に対応しています。** 760px を境に並べ方が変わります。

| 画面 | 並べ方 |
|---|---|
| 幅 > 760px かつ背が高い | 左に本文、右にパドル欄を常置。1000px 以上あると余裕が出ます |
| 幅 ≤ 760px | 本文を 1 列にし、パドルは下から出す引き出しに。打面は指でそのまま打てます |
| 高さ ≤ 520px かつ `pointer: coarse` | 同上（スマートフォンの横向き） |

横向きのスマートフォンは幅が 800〜900px あるので、幅だけで判断すると 2 列のまま
縦に潰れます。**背の低い画面も 1 列**にし、引き出しの中身も詰めて送信速度まで
画面内に収めています。パソコンの窓を低くしただけ（`pointer: fine`）は 2 列のままです。

スマートフォンでは、パドル送信タブに移ったときと実技で打鍵の番が来たときに引き出しが
自動で開き、「何を打つのか」が引き出しの上に来るよう本文も寄せます。押せるものは 44px
以上、入力欄は 16px 以上（iOS の自動拡大を避けるため）にしています。
